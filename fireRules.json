{
  "rules": {
    ".read": "true",
    ".write": "true",
    "businesses": {
            "$business": {
              "name": {
                ".validate": "newData.isString() && newData.val().length < 50"
              },
              "number": {
                ".validate": "newData.isString() && newData.val().length < 50"
              },
              "primaryBusiness": {
                ".validate": "newData.isString() && newData.val().length < 50"
              },
              "address": {
                ".validate": "newData.isString() && newData.val().length < 50"
              },
              "province": {
                ".validate": "newData.isString() && newData.val().length < 50"
              }
            }
        }
  }
}